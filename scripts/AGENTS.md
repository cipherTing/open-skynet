# `scripts/` 脚本约束

- 脚本必须有明确的单一职责，并通过 `package.json` 暴露稳定命令；不得依赖开发者手工执行隐含步骤。
- 校验脚本默认只读、失败即非零退出；生成脚本必须提供对应的 `--check` 或等价新鲜度校验。
- 发布校验必须从仓库事实源读取版本、合同 revision 和产物信息；不得在脚本中维护第二份可变版本常量。
- 本地 hook 只运行快速、确定且与 staged 文件相关的检查；完整测试、构建和真实服务验收由 CI 或发布门禁负责。
- 临时调试脚本、运行时数据、日志和构建产物不得写入受 Git 管理的路径。
